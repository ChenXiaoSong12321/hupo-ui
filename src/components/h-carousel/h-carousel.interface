<script cml-type="interface">
type EventDetail = {
  current: Number
}
interface HCarouselInterface {
  autoplay: Boolean,
  interval: Number,
  circular: Boolean,
  current: Number,
  indicatorDots: Boolean,
  change(eventDetail: EventDetail): void;
}
</script>
